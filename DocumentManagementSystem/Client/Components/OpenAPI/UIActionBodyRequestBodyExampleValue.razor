
<pre class="body-param__example microlight" style="display: block; overflow-x: auto; padding: 0.5em; background: rgb(51, 51, 51); color: white;">
<code class="language-json" style="white-space: pre;">
<span>{</span><span>@Environment.NewLine</span>
        @foreach (var property in Schema.Properties)
        {
<span>&nbsp;&nbsp;</span><span class="hljs-attr">"@property.Key"</span><span>: </span><span style="color: rgb(162, 252, 162);">"@property.Value.Type"</span>
            @if (propertyCount + 1 != Schema.Properties.Count)
                {
                propertyCount++; 
        <span>,</span><span>@Environment.NewLine</span>
                }
        }
        <span>@Environment.NewLine</span>}
</code>
</pre>

@code {
    [Parameter]
    public Schema Schema { get; set; }

    int propertyCount { get; set; }

    void Parse(Dictionary<string, Property> Properties, string tab)
    {
        
        }
}
